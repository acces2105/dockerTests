services:
  #nginx_default:
  #  build: ./docker_folder/nginx_default
  #  ports:
  #  - '80:80'
  nginx:
    build: ./docker_folder/nginx
    ports:
    - '80:80'
    volumes:
      - type: bind
        source: ./src
        target: /var/www/nginx/projects/projects_php
  php:
    build: ./docker_folder/php
  ##  ports:
  ##    - "9000:9000"
    #depends_on:
    #- web1
    #- web2

  #redis:
  #  image: redis
  #  ports:
  #    - '6379:6379'
  #web1:
  #  restart: on-failure
  #  build: ./web
  #  hostname: web1
  #  ports:
  #    - '81:5000'
  #web2:
  #  restart: on-failure
  # build: ./web
  #  hostname: web2
  #  ports:
  #    - '82:5000'
  
